# TrafficSense
TrafficSense is a CNN-based deep learning project that classifies vehicle and non-vehicle images using TensorFlow and Keras. The project analyzes overfitting by comparing training and validation accuracy across epochs and demonstrates image classification using a custom-trained CNN model.
